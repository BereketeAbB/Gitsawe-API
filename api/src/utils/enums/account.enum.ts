export enum EAccountType  {
    TELEGRAM_BOT = "TELEGRAM_BOT",
    EMAIL = "EMAIL",
}

export enum ERole {
    USER = 'USER',
    ADMIN = 'ADMIN'
}

export enum EUserStatus {
    DRAFT = "Draft",
    ACTIVE = "Active",
    INACTIVE = "Inactive",
}

export enum EAccountStatus {
    DRAFT = "Draft",
    ACTIVE = "Active",
    INACTIVE = "Inactive",
}